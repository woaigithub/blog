<h1>Sign in</h1>

<div class="row">
  <%= form_for User.new, :url => sessions_path do |f| %>
    <%= f.label :email %>
    <%= f.text_field :email %>

    <%= f.submit "sign in" %>
  <% end %>
</div>
