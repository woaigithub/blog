<h1><%= @post.title %></h1>

<div>
  Category: <%= link_to @post.category.title, @post.category %>
</div>
<div>
  <%= @post.user.nickname %> created at <%= @post.created_at.localtime.to_s(:db) %>
</div>
<div>
  <%= raw @post.content%>
</div>


<hr/>

<h3>Comments</h3>

<%= render 'comments/comment' %>

<%= render 'comments/form' %>
