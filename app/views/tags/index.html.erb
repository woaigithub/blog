<%= content_for :title do %>
-Tags
<% end %>

<h1>Listing tags</h1>

<table>
  
  <tr>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  <% @tags.each do |tag| %>
  <tr>
    <td><%= tag.title %></td>
    <td><%= link_to 'Show', tag %></td>
    <td><%= link_to 'Edit', edit_tag_path(tag) %></td>
    <td><%= link_to 'Delete', tag, method: :delete, data: { confirm: 'are your sure?' } %></td>
  </tr>
  <% end %>
</table>


<br/>

<%= link_to 'New Tag', new_tag_path %>
